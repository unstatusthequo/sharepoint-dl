---
phase: 04-resume-safety-and-failure-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sharepoint_dl/state/job_state.py
  - sharepoint_dl/downloader/engine.py
  - tests/test_state.py
  - tests/test_downloader.py
autonomous: true
requirements: [DWNL-02]

must_haves:
  truths:
    - "Interrupted-run cleanup deletes only the tracked .part file for that entry, even when other folders contain the same filename"
    - "Resume state persists enough local-path metadata to recover safely after a crash without filename-wide rglob deletion"
    - "Existing resume behavior still skips already-complete files and retries interrupted/failed work"
  artifacts:
    - path: "sharepoint_dl/state/job_state.py"
      provides: "Path-safe interrupted cleanup and persisted local output metadata"
      exports: ["JobState", "FileStatus"]
      contains: "def cleanup_interrupted"
    - path: "sharepoint_dl/downloader/engine.py"
      provides: "Downloader wiring that records the exact local output path before streaming starts"
      exports: ["download_all"]
      contains: "def download_all"
    - path: "tests/test_state.py"
      provides: "Regression tests for duplicate-filename cleanup safety"
      contains: "class TestPartCleanup"
    - path: "tests/test_downloader.py"
      provides: "Regression tests for resume wiring after path-metadata changes"
      contains: "class TestResumeSkip"
  key_links:
    - from: "sharepoint_dl/downloader/engine.py"
      to: "sharepoint_dl/state/job_state.py"
      via: "worker records exact local output path before marking an entry downloading"
      pattern: "set_status\\(.*local"
    - from: "sharepoint_dl/downloader/engine.py"
      to: "sharepoint_dl/state/job_state.py"
      via: "resume cleanup uses tracked path metadata instead of filename-wide search"
      pattern: "cleanup_interrupted\\("
---

<objective>
Repair the downloader/state contract so resume behavior is path-safe even when multiple folders contain the same filename.

Purpose: The audit gap on duplicate filenames is a correctness bug in persisted download state. This plan fixes the resume mechanics only; it deliberately stops short of Phase 5 manifest-path work and leaves user-facing CLI reporting to 04-02.
Output: path-safe resume cleanup in `JobState`, downloader wiring that persists exact local placement metadata before streaming starts, and focused regression coverage.
</objective>

<execution_context>
@/Users/richardlutkus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardlutkus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md

@sharepoint_dl/state/job_state.py
@sharepoint_dl/downloader/engine.py
@tests/test_state.py
@tests/test_downloader.py

<interfaces>
Current state contract:
```python
class JobState:
    def initialize(self, files: list[FileEntry]) -> None: ...
    def set_status(self, server_relative_url: str, status: FileStatus, **kwargs) -> None: ...
    def pending_files(self) -> list[str]: ...
    def complete_files(self) -> list[str]: ...
    def failed_files(self) -> list[tuple[str, str]]: ...
    def cleanup_interrupted(self, dest_dir: Path) -> None: ...
```

Current download orchestration contract:
```python
def download_all(
    session: requests.Session,
    files: list[FileEntry],
    dest_dir: Path,
    site_url: str,
    workers: int = 3,
    progress: Progress | None = None,
    flat: bool = False,
) -> tuple[list[str], list[tuple[str, str]]]: ...
```

Audit constraints:
- `DWNL-02` is unsatisfied because interrupted cleanup deletes `.part` files by filename only
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make interrupted cleanup exact-path safe and regression-proof</name>
  <files>sharepoint_dl/state/job_state.py, tests/test_state.py</files>
  <behavior>
    - TestPartCleanup: when two different folders contain the same filename, `cleanup_interrupted()` deletes only the tracked interrupted entry's `.part` file
    - TestPartCleanup: interrupted entries reset to `pending` after cleanup, and unrelated partials remain untouched
    - TestPartCleanup: cleanup works for both preserved-folder output and flat-output runs if exact local-path metadata was stored before interruption
    - TestInitializeIdempotent: legacy entries without the new path metadata do not trigger broad filename-wide deletion on reinitialize/resume
  </behavior>
  <action>
Refactor `sharepoint_dl/state/job_state.py` so resume cleanup is driven by tracked output-path metadata rather than `dest_dir.rglob(filename + ".part")`.

Implementation direction:
- Persist exact local output information in state as soon as a worker claims a file for download. A relative path under `dest_dir` is preferred so state remains portable across machines and reruns.
- Add a narrow helper used by `cleanup_interrupted()` that resolves the tracked `.part` path for one entry and deletes only that file.
- For backwards compatibility with old `state.json` files that do not have the new metadata, choose the safest fallback available. Do not retain the current filename-wide deletion behavior.
- Keep the current lifecycle semantics: interrupted entries become `pending` and completed entries remain untouched.

Update `tests/test_state.py` with duplicate-filename regression coverage and explicit assertions that unrelated partials survive resume cleanup.
  </action>
  <verify>
    <automated>uv run pytest tests/test_state.py -x -q</automated>
  </verify>
  <done>Resume cleanup is exact-path safe, duplicate filenames no longer collide, and state persists enough metadata to recover interrupted downloads safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire exact local placement metadata into the download lifecycle</name>
  <files>sharepoint_dl/downloader/engine.py, tests/test_downloader.py</files>
  <behavior>
    - TestResumeSkip: downloads persist the exact local placement metadata needed by future resume cleanup
    - TestResumeSkip: the new state metadata preserves the current skip-already-complete behavior on rerun
    - TestProgress: progress handling still works after the state metadata change
    - TestConcurrency: existing concurrent worker behavior remains intact after the downloader wiring change
  </behavior>
  <action>
Refactor `download_all()` in `sharepoint_dl/downloader/engine.py` so every in-flight entry records the exact local output path before streaming begins.

Implementation direction:
- Reuse the same placement logic that determines `dest_path` so the stored metadata cannot drift from the actual download target.
- Persist that metadata before the entry is marked `DOWNLOADING`, ensuring the next run can clean up safely after a crash.
- Preserve current invariants: 401/403 still halt the run quickly, completed work remains resumable, and failed entries are explicit in state.
- Update downloader tests to prove resume wiring still behaves correctly after the metadata change.
  </action>
  <verify>
    <automated>uv run pytest tests/test_downloader.py -x -q</automated>
  </verify>
  <done>The downloader records exact local placement metadata before streaming starts, and resume-related downloader regressions remain green.</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_state.py tests/test_downloader.py -x -q
</verification>

<success_criteria>
- `cleanup_interrupted()` no longer uses filename-wide deletion
- Resume-safe path metadata is persisted before streaming starts
- Duplicate filenames in different folders no longer collide during interrupted cleanup
- Existing concurrency, progress, and resume-skip behavior remain green
- All tests pass: `uv run pytest tests/test_state.py tests/test_downloader.py -x -q`
</success_criteria>

<output>
After completion, create `.planning/phases/04-resume-safety-and-failure-reporting/04-01-SUMMARY.md`
</output>
