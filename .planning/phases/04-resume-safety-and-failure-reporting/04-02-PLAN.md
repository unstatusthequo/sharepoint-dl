---
phase: 04-resume-safety-and-failure-reporting
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sharepoint_dl/cli/main.py
  - tests/test_cli.py
autonomous: true
requirements: [ENUM-03, CLI-03]

must_haves:
  truths:
    - "`sharepoint-dl download` prints the file count and total size before any transfer starts, including when `--yes` skips confirmation"
    - "Auth-expired download runs still print completeness and file-level failure summaries before exiting non-zero"
    - "The CLI preserves the current success/failure exit-code behavior while treating auth expiry as a failed run outcome"
    - "Auth-expired runs generate a partial manifest from persisted `JobState` unless `--no-manifest` is set, then print the same reporting tail as any other failed run"
  artifacts:
    - path: "sharepoint_dl/cli/main.py"
      provides: "Pre-download visibility and auth-expiry-aware end-of-run reporting"
      contains: "def download"
    - path: "tests/test_cli.py"
      provides: "Regression tests for --yes visibility and auth-expiry summary output"
      contains: "class TestDownloadAuthExpired"
  key_links:
    - from: "sharepoint_dl/cli/main.py"
      to: "sharepoint_dl/downloader/engine.py"
      via: "download command catches `AuthExpiredError`, reloads persisted `JobState`, generates partial manifest, and continues through the reporting tail"
      pattern: "download_all\\("
    - from: "sharepoint_dl/cli/main.py"
      to: "sharepoint_dl/enumerator/traversal.py"
      via: "enumeration count/size summary is printed before transfer begins"
      pattern: "enumerate_files\\("
    - from: "sharepoint_dl/cli/main.py"
      to: "sharepoint_dl/manifest/writer.py"
      via: "auth-expired runs reuse persisted state to generate partial manifest before completeness reporting"
      pattern: "generate_manifest\\("
---

<objective>
Restore the user-facing guarantees for the download command: show scope before transfer starts, and always emit explicit end-of-run reporting even when auth expires mid-stream.

Purpose: Phase 4 closes with the CLI because the remaining audited gaps are not about core download mechanics alone; they are about what the user sees and what evidence the tool emits when things go wrong.
Output: `download` prints preflight scope consistently and emits completeness/error reporting for success, ordinary failure, and auth-expiry failure.
</objective>

<execution_context>
@/Users/richardlutkus/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardlutkus/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04-resume-safety-and-failure-reporting/04-01-PLAN.md

@sharepoint_dl/cli/main.py
@sharepoint_dl/downloader/engine.py
@sharepoint_dl/manifest/writer.py
@tests/test_cli.py

<interfaces>
Current CLI behaviors to replace:
```python
if not yes:
    console.print(f"Download {count} files ...")

try:
    completed, failed = download_all(...)
except AuthExpiredError:
    console.print("Session expired during download ...")
    raise typer.Exit(code=1)
```

Audit constraints:
- `ENUM-03` remains partial because `--yes` skips the pre-download count/size output
- `CLI-03` remains partial because auth-expiry exits before completeness/error summary reporting
- the cross-phase gap also notes that the CLI currently aborts before the post-download reporting tail runs
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Print pre-download scope unconditionally before transfers begin</name>
  <files>sharepoint_dl/cli/main.py, tests/test_cli.py</files>
  <behavior>
    - TestDownloadConfirmation: without `--yes`, the command prints file count, total size, and destination before prompting
    - TestDownloadConfirmation: with `--yes`, the same file count and total size are still printed before downloads start
    - TestDownloadCommand: existing argument wiring (`--root-folder`, `--workers`, `--flat`, `--no-manifest`) remains intact
  </behavior>
  <action>
Refactor `sharepoint_dl/cli/main.py` so the download preflight summary is printed unconditionally before transfer begins.

Implementation direction:
- Move the count/size/destination summary out of the confirmation-only branch.
- Keep the confirmation prompt when `--yes` is absent, but make it additive rather than the only place the scope is shown.
- Preserve the current empty-folder early exit and other validation behaviors.

Update `tests/test_cli.py` so the `--yes` path asserts the pre-download scope appears in output, not just the success summary after transfer.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "DownloadConfirmation or DownloadCommand" -x -q</automated>
  </verify>
  <done>The CLI always shows the user what will be downloaded before any transfer starts, including scripted `--yes` runs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit completeness and failure reporting even on auth-expired runs</name>
  <files>sharepoint_dl/cli/main.py, tests/test_cli.py</files>
  <behavior>
    - TestDownloadAuthExpired: auth-expired runs exit with code 1 after printing the completeness report, failed-file summary, and re-auth guidance
    - TestErrorSummary: auth-expired failures still appear as explicit file-level failures in the same reporting path used for other failed downloads
    - TestDownloadExitCode: success remains exit 0 and any failure path remains exit 1
    - TestManifestIntegration: with a real persisted `state.json` under `tmp_path`, auth-expired runs generate `manifest.json` from that state unless `--no-manifest` is set
  </behavior>
  <action>
Update the `download` command so `AuthExpiredError` becomes one failed-run outcome, not an early process abort that bypasses reporting.

Implementation direction:
- Catch `AuthExpiredError`, reload `JobState(dest)`, generate a partial manifest from that persisted state unless `--no-manifest` is set, and continue through the same completeness/error-summary tail used for other failed runs.
- Reuse the existing completeness and failed-download reporting path so auth-expiry is not a one-off branch with weaker evidence.
- Preserve the explicit re-authenticate guidance, but print it after the run summary instead of before the summary exists.
- Keep exit-code semantics simple: zero only when all files completed, one for any failure or auth-expired run.
- Make the manifest rule explicit for this phase: failed/auth-expired runs still generate a partial manifest from persisted state unless `--no-manifest` is set. Phase 5 will correct path accuracy, but Phase 4 closes the integration gap by keeping the reporting artifact path alive.

Update `tests/test_cli.py` to cover auth-expired summary/reporting output with a state-backed integration test that pre-populates a real `state.json` in `tmp_path`, invokes the CLI against that destination, and asserts `manifest.json`, completeness output, failed-file detail, and re-auth guidance all appear as expected.
  </action>
  <verify>
    <automated>uv run pytest tests/test_cli.py -k "DownloadAuthExpired or ErrorSummary or DownloadExitCode or ManifestIntegration" -x -q</automated>
  </verify>
  <done>Auth-expired download runs produce the same quality of explicit completion/failure evidence as any other failed run, and the CLI exits non-zero only after that reporting is emitted.</done>
</task>

</tasks>

<verification>
uv run pytest tests/test_cli.py -x -q
</verification>

<success_criteria>
- Pre-download count/size output appears before transfer for both interactive and `--yes` runs
- The CLI uses one reporting tail for success, ordinary failures, and auth-expiry failures
- Auth-expired runs generate a partial manifest from persisted state unless `--no-manifest` is set
- Auth-expired runs still print completeness and file-level failure summaries before exit
- Success remains exit 0; any failed/auth-expired run remains exit 1
- All tests pass: `uv run pytest tests/test_cli.py -x -q`
</success_criteria>

<output>
After completion, create `.planning/phases/04-resume-safety-and-failure-reporting/04-02-SUMMARY.md`
</output>
