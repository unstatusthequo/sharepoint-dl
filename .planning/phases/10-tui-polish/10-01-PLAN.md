---
phase: 10-tui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sharepoint_dl/manifest/writer.py
  - sharepoint_dl/downloader/engine.py
  - tests/test_manifest.py
  - tests/test_downloader.py
autonomous: true
requirements: [FOR-02, UX-06]

must_haves:
  truths:
    - "After download, manifest.csv exists alongside manifest.json with correct columns"
    - "CSV contains ALL files (complete + failed) with Status column"
    - "Per-file progress bars show elapsed time since that specific file started, not session time"
    - "Overall progress bar still shows session-wide elapsed time"
  artifacts:
    - path: "sharepoint_dl/manifest/writer.py"
      provides: "CSV generation inside generate_manifest()"
      contains: "csv.writer"
    - path: "sharepoint_dl/downloader/engine.py"
      provides: "Per-file elapsed timer in progress display"
      contains: "time.monotonic"
  key_links:
    - from: "sharepoint_dl/manifest/writer.py"
      to: "manifest.csv"
      via: "csv.writer inside generate_manifest()"
      pattern: "manifest\\.csv"
    - from: "sharepoint_dl/downloader/engine.py"
      to: "Rich Progress"
      via: "TextColumn with task.fields[elapsed]"
      pattern: "task\\.fields\\[.elapsed.\\]"
---

<objective>
Add CSV manifest export and per-file elapsed timer to the download engine.

Purpose: FOR-02 gives forensic users an Excel-openable manifest; UX-06 gives accurate per-file timing instead of misleading session-wide elapsed time on worker progress bars.
Output: Updated writer.py with CSV generation, updated engine.py with per-file elapsed timer, passing tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-tui-polish/10-CONTEXT.md

@sharepoint_dl/manifest/writer.py
@sharepoint_dl/downloader/engine.py
@tests/test_manifest.py
@tests/test_downloader.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From sharepoint_dl/manifest/writer.py:
```python
def generate_manifest(
    state: JobState,
    dest_dir: Path,
    source_url: str,
    root_folder: str,
    *,
    flat: bool = False,
) -> Path:
```

From sharepoint_dl/state/job_state.py:
```python
class FileStatus(str, Enum):
    PENDING = "pending"
    DOWNLOADING = "downloading"
    COMPLETE = "complete"
    FAILED = "failed"
```

From sharepoint_dl/downloader/engine.py:
```python
def _make_progress() -> Progress:
    # Returns Rich Progress with SpinnerColumn, TextColumn, BarColumn, etc.

def download_all(
    session, files, dest_dir, site_url,
    workers=3, progress=None, flat=False, throttle=None,
    on_auth_expired=None, files_dir=None,
) -> tuple[list[str], list[tuple[str, str]]]:
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CSV manifest generation to writer.py</name>
  <files>sharepoint_dl/manifest/writer.py, tests/test_manifest.py</files>
  <read_first>sharepoint_dl/manifest/writer.py, tests/test_manifest.py, sharepoint_dl/state/job_state.py</read_first>
  <behavior>
    - Test 1: generate_manifest() creates manifest.csv alongside manifest.json in dest_dir
    - Test 2: CSV has header row: filename,local_path,size_bytes,sha256,status,error,downloaded_at
    - Test 3: Complete files have status="COMPLETE", error="" (blank), and full sha256 hash
    - Test 4: Failed files have status="FAILED", error contains the error reason, sha256="" (blank)
    - Test 5: CSV rows are sorted by server_relative_url (same order as JSON)
    - Test 6: Filenames with commas/quotes are properly escaped by csv.writer
  </behavior>
  <action>
    Per D-01 through D-05:
    1. Add `import csv` to writer.py (stdlib, no new dependencies — per D-05).
    2. After the JSON atomic write block in `generate_manifest()`, add CSV generation.
    3. Build CSV rows from both `files_list` (status=COMPLETE) and `failed_list` (status=FAILED), sorted by server_relative_url.
    4. CSV columns per D-03: filename, local_path, size_bytes, sha256, status, error, downloaded_at.
       - For complete files: error is empty string, sha256 is full hex digest.
       - For failed files: sha256 is empty string, size_bytes is 0, local_path is empty, downloaded_at is empty.
    5. Write CSV atomically (same .tmp + rename pattern as JSON) to `dest_dir / "manifest.csv"`.
    6. Per D-04: CSV generation is automatic — no user prompt, no flag.
    7. Use `csv.writer` with default quoting (QUOTE_MINIMAL) — stdlib handles commas/quotes.
  </action>
  <verify>
    <automated>cd /Users/richardlutkus/Documents/Programming/sharepoint_dl && uv run pytest tests/test_manifest.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "csv" sharepoint_dl/manifest/writer.py` returns at least 2 (import + writer usage)
    - `grep -c "manifest.csv" sharepoint_dl/manifest/writer.py` returns at least 1
    - `grep -c "COMPLETE" sharepoint_dl/manifest/writer.py` returns at least 1
    - `grep -c "FAILED" sharepoint_dl/manifest/writer.py` returns at least 1
    - `grep -c "manifest.csv" tests/test_manifest.py` returns at least 1
    - All tests in tests/test_manifest.py pass
  </acceptance_criteria>
  <done>generate_manifest() writes manifest.csv with all 7 columns, complete+failed rows, proper escaping, and atomic write. Tests verify CSV content and structure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-file elapsed timer to progress bars</name>
  <files>sharepoint_dl/downloader/engine.py, tests/test_downloader.py</files>
  <read_first>sharepoint_dl/downloader/engine.py, tests/test_downloader.py</read_first>
  <behavior>
    - Test 1: _make_progress() returns a Progress with an "elapsed" field column for worker tasks
    - Test 2: Overall task still uses TimeElapsedColumn (session-wide timer unchanged)
    - Test 3: Worker task elapsed field is formatted as "Xs" or "Xm Xs" (human-readable)
    - Test 4: When a worker picks up a new file, its elapsed timer resets to 0
  </behavior>
  <action>
    Per D-12, D-13, D-14:
    1. In `_make_progress()`, replace the single `TimeElapsedColumn()` with a `TextColumn("{task.fields[elapsed]}")`.
       The overall task will have its own elapsed tracking via a field, and worker tasks will have per-file elapsed.
    2. Add a helper function `_format_elapsed(seconds: float) -> str` that formats elapsed time:
       - Under 60s: "12s"
       - 60s+: "2m 15s"
       - Under 1s: "0s"
    3. In the `worker()` closure inside `download_all()`:
       - Record `file_start = time.monotonic()` when the worker picks up a new file.
       - In the existing progress.update call when resetting the worker task for a new file, set `elapsed="0s"` in fields.
       - Modify the `on_chunk` callback to compute `elapsed = time.monotonic() - file_start` and update the field: `progress.update(_t, advance=n, elapsed=_format_elapsed(elapsed))`.
    4. For the overall task:
       - Record `overall_start = time.monotonic()` before entering the ThreadPoolExecutor.
       - In the `on_chunk` callback, also update the overall task's elapsed field: `progress.update(_o, advance=n, elapsed=_format_elapsed(time.monotonic() - overall_start))`.
       - This replaces the session-wide TimeElapsedColumn with an equivalent field-based approach that works for both overall and worker tasks.
    5. Initialize all tasks with `elapsed="0s"` in their fields dict.
    6. The existing `status` field remains — both `status` and `elapsed` are in task.fields.
  </action>
  <verify>
    <automated>cd /Users/richardlutkus/Documents/Programming/sharepoint_dl && uv run pytest tests/test_downloader.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "elapsed" sharepoint_dl/downloader/engine.py` returns at least 4
    - `grep -c "_format_elapsed" sharepoint_dl/downloader/engine.py` returns at least 2 (def + usage)
    - `grep -c "time.monotonic" sharepoint_dl/downloader/engine.py` returns at least 2
    - `grep -q "TimeElapsedColumn" sharepoint_dl/downloader/engine.py` returns 0 (removed from _make_progress columns list) OR TimeElapsedColumn is only imported but not used in _make_progress
    - All tests in tests/test_downloader.py pass
  </acceptance_criteria>
  <done>Per-file progress bars show elapsed time since that file started downloading. Overall progress shows session-wide elapsed. Worker elapsed resets when picking up a new file.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/test_manifest.py tests/test_downloader.py -x -q` — all pass
- `grep "manifest.csv" sharepoint_dl/manifest/writer.py` — CSV file path referenced
- `grep "_format_elapsed" sharepoint_dl/downloader/engine.py` — elapsed helper exists
</verification>

<success_criteria>
1. generate_manifest() produces both manifest.json and manifest.csv atomically
2. CSV has 7 columns: filename, local_path, size_bytes, sha256, status, error, downloaded_at
3. CSV includes COMPLETE and FAILED entries with appropriate field values
4. Per-file progress bars show elapsed time since file download started
5. Overall progress bar shows session-wide elapsed time
6. All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/10-tui-polish/10-01-SUMMARY.md`
</output>
